diff --git a/qcacld-3.0/core/hdd/src/wlan_hdd_main.c b/qcacld-3.0/core/hdd/src/wlan_hdd_main.c
--- a/qcacld-3.0/core/hdd/src/wlan_hdd_main.c
+++ b/qcacld-3.0/core/hdd/src/wlan_hdd_main.c
@@ -130,6 +130,8 @@
 #include <cdp_txrx_handle.h>
 #include <qca_vendor.h>
 #include "wlan_pmo_ucfg_api.h"
+#include "wlan_pmo_priv.h"
+#include "wlan_pmo_objmgr.h"
 #include "sir_api.h"
 #include "os_if_wifi_pos.h"
 #include "wifi_pos_api.h"
@@ -255,6 +257,8 @@
 #include "wlan_p2p_ucfg_api.h"
 #include "wifi_pos_api.h"
 #include "wlan_mgmt_rx_srng_ucfg_api.h"
 
+void dp_try_set_rps_cpu_mask(struct wlan_objmgr_psoc *psoc);
+
 #ifdef MULTI_CLIENT_LL_SUPPORT
 #define WLAM_WLM_HOST_DRIVER_PORT_ID 0xFFFFFF
 #endif
@@ -4069,7 +4073,7 @@ void hdd_update_hw_sw_info(struct hdd_context *hdd_ctx)
  *
  * Return: none
  */
-static void
+static __always_inline void
 hdd_update_cds_ac_specs_params(struct hdd_context *hdd_ctx)
 {
 	uint8_t tx_sched_wrr_param[TX_SCHED_WRR_PARAMS_NUM] = {0};
@@ -5252,31 +5252,21 @@ int hdd_wlan_start_modules(struct hdd_context *hdd_ctx, bool reinit)
 			goto power_down;
 		}
 
-		status = ol_cds_init(qdf_dev, hif_ctx);
-		if (status != QDF_STATUS_SUCCESS) {
-			hdd_err("No Memory to Create BMI Context; status: %d",
-				status);
-			ret = qdf_status_to_os_return(status);
-			goto hif_close;
-		}
-
 		if (hdd_get_conparam() == QDF_GLOBAL_EPPING_MODE) {
 			status = epping_open();
 			if (status) {
 				hdd_err("Failed to open in epping mode: %d",
 					status);
 				ret = -EINVAL;
-				goto cds_free;
+				goto hif_close;
 			}
 
 			status = epping_enable(qdf_dev->dev, false);
 			if (status) {
 				hdd_err("Failed to enable in epping mode : %d",
 					status);
 				epping_close();
-				goto cds_free;
+				goto hif_close;
 			}
 
 			hdd_info("epping mode enabled");
@@ -5286,14 +5276,8 @@ int hdd_wlan_start_modules(struct hdd_context *hdd_ctx, bool reinit)
 		}
 
-		if (pld_is_ipa_offload_disabled(qdf_dev->dev))
-			ucfg_ipa_set_pld_enable(false);
-
-		if (pld_is_ipa_shared_smmu_enable(qdf_dev->dev))
-			ucfg_ipa_set_shared_smmu_enable(true);
-
-		ucfg_ipa_component_config_update(hdd_ctx->psoc);
+		pld_is_ipa_offload_disabled(qdf_dev->dev);
+		pld_is_ipa_shared_smmu_enable(qdf_dev->dev);
 
 		hdd_update_cds_ac_specs_params(hdd_ctx);
 
@@ -5308,14 +5292,14 @@ int hdd_wlan_start_modules(struct hdd_context *hdd_ctx, bool reinit)
 			hdd_err("Failed to Open legacy components; status: %d",
 				status);
 			ret = qdf_status_to_os_return(status);
-			goto ipa_component_free;
+			goto hif_close;
 		}
 
 		ret = hdd_update_config(hdd_ctx);
 		if (ret) {
 			hdd_err("Failed to update configuration; errno: %d",
 				ret);
-			goto ipa_component_free;
+			goto hif_close;
 		}
 
 		status = wbuff_module_init();
@@ -5537,12 +5521,6 @@ psoc_close:
 	wlan_global_lmac_if_close(hdd_ctx->psoc);
 	cds_deinit_ini_config();
 
-ipa_component_free:
-	ucfg_ipa_component_config_free();
-
-cds_free:
-	ol_cds_free();
-
 hif_close:
 	hif_ctx = cds_get_context(QDF_MODULE_ID_HIF);
 	hdd_hif_close(hdd_ctx, hif_ctx);
@@ -15225,7 +15203,7 @@ void hdd_psoc_idle_timer_stop(struct hdd_context *hdd_ctx)
 
 void hdd_psoc_idle_timer_stop(struct hdd_context *hdd_ctx)
 {
-	qdf_delayed_work_stop_sync(&hdd_ctx->psoc_idle_timeout_work);
+	cancel_delayed_work_sync(&hdd_ctx->psoc_idle_timeout_work.dwork);
 	hdd_allow_suspend(WIFI_POWER_EVENT_WAKELOCK_IFACE_CHANGE_TIMER);
 	hdd_debug("Stopped psoc idle timer");
 }
@@ -19017,7 +18995,7 @@ QDF_STATUS hdd_psoc_create_vdevs(struct hdd_context *hdd_ctx)
 		return status;
 	}
 
-	ucfg_dp_try_set_rps_cpu_mask(hdd_ctx->psoc);
+	dp_try_set_rps_cpu_mask(hdd_ctx->psoc);
 
 	if (driver_mode != QDF_GLOBAL_FTM_MODE &&
 	    driver_mode != QDF_GLOBAL_EPPING_MODE)
@@ -22357,9 +22335,11 @@ static int hdd_update_dp_config(struct hdd_context *hdd_ctx)
  */
 int hdd_update_config(struct hdd_context *hdd_ctx)
 {
+	struct pmo_psoc_priv_obj *pmo_psoc_ctx;
 	int ret;
 
-	if (ucfg_pmo_is_ns_offloaded(hdd_ctx->psoc))
+	pmo_psoc_ctx = pmo_psoc_get_priv(hdd_ctx->psoc);
+	if (pmo_psoc_ctx && pmo_psoc_ctx->psoc_cfg.ns_offload_enable_static)
 		hdd_ctx->ns_offload_enable = true;
 
 	hdd_update_ol_config(hdd_ctx);
diff --git a/qcacld-3.0/core/hdd/src/wlan_hdd_driver_ops.c b/qcacld-3.0/core/hdd/src/wlan_hdd_driver_ops.c
--- a/qcacld-3.0/core/hdd/src/wlan_hdd_driver_ops.c
+++ b/qcacld-3.0/core/hdd/src/wlan_hdd_driver_ops.c
@@ -44,6 +44,7 @@
 #include "wlan_ipa_ucfg_api.h"
 #include "wlan_hdd_debugfs.h"
 #include "cfg_ucfg_api.h"
+#include "wlan_dp_prealloc.h"
+#include "wlan_pmo_main.h"
 #include <linux/suspend.h>
 #include <qdf_notifier.h>
 #include <qdf_hang_event_notifier.h>
@@ -610,7 +612,7 @@ int hdd_hif_open(struct device *dev, void *bdev, const struct hif_bus_id *bid,
 	hdd_hif_set_ce_max_yield_time(
 				hif_ctx, bus_type,
 				cfg_get(hdd_ctx->psoc,
 					CFG_DP_CE_SERVICE_MAX_YIELD_TIME));
-	ucfg_pmo_psoc_set_hif_handle(hdd_ctx->psoc, hif_ctx);
+	pmo_core_psoc_set_hif_handle(hdd_ctx->psoc, hif_ctx);
 	ucfg_dp_set_hif_handle(hdd_ctx->psoc, hif_ctx);
 	hif_set_ce_service_max_rx_ind_flush(hif_ctx,
 				cfg_get(hdd_ctx->psoc,
@@ -646,7 +648,7 @@ void hdd_hif_close(struct hdd_context *hdd_ctx, void *hif_ctx)
 	hdd_deinit_cds_hif_context();
 	hif_close(hif_ctx);
 
-	ucfg_pmo_psoc_set_hif_handle(hdd_ctx->psoc, NULL);
+	pmo_core_psoc_set_hif_handle(hdd_ctx->psoc, NULL);
 }
 
 /**
@@ -801,8 +802,7 @@ static int __hdd_soc_probe(struct device *dev,
 		goto assert_fail_count;
 	}
 
-	status = ucfg_dp_prealloc_init((struct cdp_ctrl_objmgr_psoc *)
-					hdd_ctx->psoc);
+	status = dp_prealloc_init((struct cdp_ctrl_objmgr_psoc *)hdd_ctx->psoc);
 
 	if (status != QDF_STATUS_SUCCESS) {
 		errno = qdf_status_to_os_return(status);
@@ -833,7 +833,7 @@ wlan_exit:
 	hdd_wlan_exit(hdd_ctx);
 
 hdd_context_destroy:
-	ucfg_dp_prealloc_deinit();
+	dp_prealloc_deinit();
 
 dp_prealloc_fail:
 	hdd_context_destroy(hdd_ctx);
@@ -1039,7 +1039,7 @@ static void __hdd_soc_remove(struct device *dev)
 	cds_set_driver_in_bad_state(false);
 	cds_set_unload_in_progress(false);
 
-	ucfg_dp_prealloc_deinit();
+	dp_prealloc_deinit();
 
 	pr_info("%s: Driver De-initialized\n", WLAN_MODULE_NAME);
 }
